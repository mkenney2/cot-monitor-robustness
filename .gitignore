# Artifacts are downloaded from the Hugging Face dataset, not committed.
data/
results/
logs/
__pycache__/
*.pyc
.ipynb_checkpoints/
hf_staging/
# Notebook review dumps contain transcript text (GPQA-derived items live only
# in the gated dataset, not in this repo).
review/

# figures/ is tracked: all 13 figures regenerate from results/ via src/10,11,15,17,24.
# Notebook-generated figure copies (rewritten on every notebook run) stay untracked.
figures/nb_*.png
